# 智能错题簿 - 项目技术规范

## 1. 技术栈

| 类别 | 技术选择 |
|-----|---------|
| 前端框架 | Next.js 14 (App Router) |
| 语言 | TypeScript |
| 样式方案 | Tailwind CSS + 自定义主题 |
| 后端/数据库 | Supabase (PostgreSQL + Auth) |
| 图片存储 | Supabase Storage |
| AI识别 | 百度文字识别 API (可选) |
| 表单处理 | React Hook Form |
| 状态管理 | Zustand |

---

## 2. UI/UX 设计规范 - 新海诚风格

### 2.1 色彩系统

```css
/* 主色调 - 天空蓝 */
--color-sky: #87CEEB;
--color-sky-light: #E0F4FF;
--color-sky-dark: #5D9BCE;

/* 夕阳色调 */
--color-sunset: #FF7E5F;
--color-sunset-light: #FFB088;
--color-sunset-warm: #FEB47B;

/* 辅助色 */
--color-cloud: #FFFFFF;
--color-cloud-shadow: #F0F5FA;
--color-night: #1A1A2E;

/* 功能色 */
--color-success: #7BC67B;
--color-warning: #F5A623;
--color-error: #E74C3C;
--color-info: #3498DB;

/* 中性色 */
--color-text-primary: #2C3E50;
--color-text-secondary: #7F8C8D;
--color-text-light: #BDC3C7;
--color-border: #E8EEF2;
```

### 2.2 背景设计

#### 主页/登录页
- **渐变天空背景**：从浅蓝到暖橙的渐变（模拟黄昏/夕阳）
- **云朵装饰**：半透明白色云朵浮层，缓慢飘动动画
- **光晕效果**：柔和的光晕，营造温暖氛围

#### 内容页
- **浅色主背景**：淡蓝灰白色调
- **卡片阴影**：柔和的投影，模拟光影效果
- **渐变装饰条**：顶部/侧边使用天空蓝渐变装饰

### 2.3 字体

```css
/* 标题 - 清新现代 */
font-family: 'Noto Sans SC', system-ui, sans-serif;

/* 正文 - 易读舒适 */
font-family: 'Noto Sans SC', system-ui, sans-serif;

/* 数字/代码 */
font-family: 'JetBrains Mono', monospace;
```

### 2.4 组件风格

#### 按钮
- 圆角：8px - 12px
- 悬停效果：轻微上浮 + 阴影加深
- 渐变背景（主按钮）

#### 卡片
- 圆角：16px
- 阴影：柔和的投影 `0 4px 20px rgba(135, 206, 235, 0.15)`
- 悬停：轻微放大 + 阴影加深

#### 输入框
- 圆角：8px
- 边框：浅灰色
- 聚焦：天空蓝边框 + 轻微发光

#### 标签/徽章
- 学科标签：按学科区分颜色
- 知识点标签：浅蓝底色

### 2.5 动画效果

- **页面过渡**：淡入淡出 + 轻微上移
- **卡片悬停**：transform: translateY(-4px)
- **云朵飘动**：CSS 动画，缓慢移动
- **按钮点击**：轻微压缩效果
- **加载状态**：脉冲动画

### 2.6 响应式断点

| 断点 | 宽度 | 布局 |
|-----|-----|-----|
| mobile | < 640px | 单列 |
| tablet | 640px - 1024px | 双列 |
| desktop | > 1024px | 多列/侧边栏 |

---

## 3. 页面结构

### 3.1 路由设计

```
/                     - 首页/引导页
/login                - 登录
/register             - 注册
/dashboard            - 控制台（登录后）
/wrong-questions      - 错题列表
/wrong-questions/new  - 新增错题
/wrong-questions/[id] - 错题详情/编辑
/knowledge           - 知识点管理
/statistics          - 统计分析
/profile             - 个人设置
```

### 3.2 页面布局

#### 登录/注册页
- 全屏天空渐变背景
- 居中登录卡片
- 云朵装饰动画

#### 主布局
- 顶部导航栏（固定）
- 左侧边栏（可折叠）
- 主内容区域

---

## 4. 数据库设计 (Supabase)

### 4.1 表结构

#### users (用户表)
```sql
id: uuid (PK)
email: string
name: string
role: enum ('student', 'teacher', 'parent')
avatar_url: string
created_at: timestamptz
updated_at: timestamptz
```

#### wrong_questions (错题表)
```sql
id: uuid (PK)
user_id: uuid (FK -> users.id)
subject: enum ('chinese', 'math', 'english', 'physics', 'chemistry')
content: text
content_image_url: string
correct_answer: text
wrong_answer: text
error_reason: text
analysis: text
chapter: string
difficulty: enum ('easy', 'medium', 'hard')
mastery_level: enum ('unfamiliar', 'normal', 'mastered')
created_at: timestamptz
updated_at: timestamptz
```

#### knowledge_points (知识点表)
```sql
id: uuid (PK)
user_id: uuid (FK -> users.id)
subject: enum ('chinese', 'math', 'english', 'physics', 'chemistry')
name: string
description: text
created_at: timestamptz
```

#### question_tags (错题-知识点关联表)
```sql
id: uuid (PK)
question_id: uuid (FK -> wrong_questions.id)
tag_id: uuid (FK -> knowledge_points.id)
```

---

## 5. 功能模块详细设计

### 5.1 认证模块
- 邮箱/密码注册登录
- 密码重置
- 登录状态保持
- 角色切换

### 5.2 错题管理模块
- 错题列表（分页、筛选、排序）
- 新增错题（表单）
- 编辑错题
- 删除错题
- 批量操作

### 5.3 知识点模块
- 知识点列表
- 添加知识点
- 错题关联知识点
- 知识点统计

### 5.4 统计分析模块
- 错题数量趋势
- 各学科错题分布
- 知识点掌握雷达图
- 薄弱知识点排行

### 5.5 拍照识别模块（可选）
- 图片上传
- 调用百度API识别
- 识别结果确认

---

## 6. 开发计划

### Phase 1: 基础架构
- [x] 创建 Next.js 项目
- [x] 配置 Tailwind CSS
- [x] 设置 Supabase
- [x] 创建数据库表

### Phase 2: 核心功能
- [ ] 用户认证
- [ ] 错题 CRUD
- [ ] 知识点管理

### Phase 3: 增强功能
- [ ] 统计分析
- [ ] 拍照识别
- [ ] 响应式优化

---

## 7. 环境变量

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 百度AI (可选)
BAIDU_API_KEY=
BAIDU_SECRET_KEY=
```

---

## 8. 组件目录结构

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 认证页面组
│   │   ├── login/
│   │   └── register/
│   ├── (main)/            # 主页面组
│   │   ├── dashboard/
│   │   ├── wrong-questions/
│   │   ├── knowledge/
│   │   ├── statistics/
│   │   └── profile/
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── ui/                # 基础UI组件
│   ├── layout/           # 布局组件
│   └── features/         # 功能组件
├── lib/
│   ├── supabase.ts       # Supabase 客户端
│   └── utils.ts
└── styles/
    └── globals.css        # 全局样式
```
